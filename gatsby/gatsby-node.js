function turnPizzasIntoPages(params) {
  // Get a template for this page
  
  // Query all pizzas
  // Loop over each and create a page for that pizza
}

export async function createPages(params) {
  // Create pages dynamically
  // 1. Pizzas
  // 2. Toppings
  // 3. Slicemasters
}
